age = int(input("Age: ")) # has to be converted to an integer so that comparisons can work because strings and integers cant be compared

print(age >= 18)

club = input("Club: ")

print(club == "Chelsea") # = is for assignment, == is for comparison